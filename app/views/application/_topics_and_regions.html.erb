
<h2>Topics:</h2>

<% Topic.all.each do |topic| %>
  <p><%= topic.name %></p>
  <%= link_to "Remove", topic_path(topic.id), method: :delete, data: {confirm: "Are you sure you want to delete this topic?" }, class: "button radius" %>
<% end %>



<h2>Regions:</h2>
